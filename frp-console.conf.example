# FRP Console 配置文件示例
# =============================================================================
# ⚠️ 安全警告 ⚠️
# =============================================================================
# 此文件包含敏感配置项的示例值。
# 在生产环境中使用时，请务必修改以下配置：
#   1. ADMIN_PASSWORD - 管理员密码（必须修改为强密码）
#   2. SECRET_KEY - Flask 密钥（必须修改为随机长字符串）
#
# 建议操作：
#   1. 复制此文件为 frp-console.conf
#   2. 修改所有敏感配置项
#   3. 确保配置文件权限设置为 600 (chmod 600 frp-console.conf)
#   4. 不要将实际配置文件提交到版本控制
# =============================================================================

# 服务端口
PORT=7600

# 管理员账号
ADMIN_USER=admin

# ⚠️ 必须修改：管理员密码（至少8位，包含大小写字母、数字和特殊字符）
# 示例：MyStr0ng!P@ssw0rd
ADMIN_PASSWORD=ChangeMe123!@#

# ⚠️ 必须修改：Flask Secret Key（用于 Session 加密，建议使用 32 位以上随机字符串）
# 可以使用以下命令生成：openssl rand -hex 32
SECRET_KEY=ChangeThisSecretKeyInProduction
